# Track smaller JSON and data files with LFS
data/urls-list/** filter=lfs diff=lfs merge=lfs -text
*.jsonl filter=lfs diff=lfs merge=lfs -text

# Large files should be excluded via .gitignore instead of LFS
# *.sqlite3
# *.bin
# *.pickle
# data/embeddings/**
# data/chunks/**
# data/raw/**
